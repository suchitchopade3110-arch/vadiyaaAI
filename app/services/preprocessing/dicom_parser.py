def parse_dicom(file_path: str):
    """
    DICOM parsing slot.

    Phase 2 owner should replace this with pydicom pixel extraction and metadata
    normalization.
    """
    raise NotImplementedError("Data/Preprocessing engineer implements DICOM parsing")

