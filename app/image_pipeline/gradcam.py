import numpy as np

def generate_gradcam(roi: np.ndarray, label: str) -> str:
    """
    Generate GradCAM heatmap for the classified ROI.
    NOTE: Placeholder for Phase 2 implementation.
    Returns the path to the saved heatmap image.
    """
    # In a real scenario:
    # heatmap = grad_cam_logic(model, roi, label)
    # path = save_heatmap(heatmap)
    # return path
    return ""
